docker network create my-network

docker run --name my-database --network my-network -d -v my-vol:/var/lib/mysql -p 3306 -e MYSQL_ROOT_PASSWORD=EdJan  mysql 
docker exec -it my-database mysql -u root -p
CREATE DATABASE wordpress;



docker run --name my-wordpress --network my-network -d --mount type=bind,src=/opt/wordpress,dst=/var/www/wordpress -p 80:80  -e WORDPRESS_DB_HOST=my-database -e WORDPRESS_DB_USER=root -e WORDPRESS_DB_PASSWORD=EdJan -e WORDPRESS_DB_NAME=wordpress wordpress

